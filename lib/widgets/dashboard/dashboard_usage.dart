import 'package:flutter/material.dart';

class DashboardUsage extends StatelessWidget {
  const DashboardUsage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Column(
          children: [],
        )
      ],
    );
  }
}
